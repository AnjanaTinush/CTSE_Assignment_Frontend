# Use the Vite dev server proxy for local development.
# VITE_API_BASE_URL is intentionally left unset so resolveApiBaseUrl()
# returns "/api" and all requests are forwarded through the Vite proxy.

# VITE_API_BASE_URL=

# Vite proxy target: forward /api/* to the live AKS API Gateway
VITE_API_PROXY_TARGET=http://20.198.193.186.sslip.io
